.env
*.env
!.env.example
.env.backup
.env.local
.env.prod
.env.real
**/venv/
**/.venv/
__pycache__/
*.pyc
.gitignore_bak/
restep2.sh

# Container-generated default configs (root-owned)
pi/config/crowdsec/notifications/
pi/config/crowdsec/webhook-relay/__init__.py

# Secrets — contain real credentials (use .template versions instead)
windows/config/authelia/users_database.yml
pi/config/traefik/dynamic.yml
windows/config/alertmanager/alertmanager.yml

# Tinybot runtime data (state contains chat IDs, logs are ephemeral)
**/tinybot/state/
**/tinybot/logs/
**/tinybot/queue/

# TLS certificates and keys
*.key
*.pem

# Backup archives
windows/backups/
pi/backups/
# CI trigger
# trigger CI v2
# CI trigger self-hosted
# CI trigger pi label
